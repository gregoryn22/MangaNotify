# -----------------------------------------------------------------------------
# MangaNotify Example Configuration
# Copy this file to `.env` and fill in your own values where needed.
# -----------------------------------------------------------------------------

# Host port to expose the web UI (default 8999)
HOST_PORT=8999

# API base (usually leave default)
MANGABAKA_BASE=https://api.mangabaka.dev

# File paths inside the container
WATCHLIST_FILE=/data/watchlist.json

# Poll interval (minutes)
WATCH_POLL_MINUTES=10

# -----------------------------------------------------------------------------
# Optional: Pushover credentials for notifications
# Comment out or leave blank if not using Pushover
# -----------------------------------------------------------------------------
#PUSHOVER_USER_KEY=your_pushover_user_key_here
#PUSHOVER_APP_TOKEN=your_pushover_app_token_here
